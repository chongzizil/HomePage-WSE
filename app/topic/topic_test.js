'use strict';

describe('myApp.result module', function () {

  beforeEach(module('myApp.result'));

  describe('result controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var resultCtrl = $controller('ResultCtrl');
      expect(resultCtrl).toBeDefined();
    }));

  });
});